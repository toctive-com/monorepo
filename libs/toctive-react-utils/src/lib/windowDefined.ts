/**
 * If the document object is defined and has a dir property, return true, otherwise return false.
 *
 * @version 1.0.0
 * @returns The function isWindowDefined is being returned.
 */
export function isWindowDefined() {
  return typeof document !== 'undefined';
}
